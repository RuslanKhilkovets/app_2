import {Screen, Categories} from '@/components';
import {View} from 'react-native';

const MainScreen = () => {
  return (
    <View style={{flex: 1}}>
      <Categories />
    </View>
  );
};

export default MainScreen;
