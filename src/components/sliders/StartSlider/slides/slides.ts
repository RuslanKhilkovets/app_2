import {Slide1, Slide2, Slide3} from '@/components';

const slides = [{component: Slide1}, {component: Slide2}, {component: Slide3}];

const slidesLength = slides.length;

export default {
  slides,
  slidesLength,
};
